// read a num and print if it is prime or not
#include <stdio.h>

int main() {
    int i,n=0,c;
    scanf("%d",&n);
    for(int i=1;i<=n;i++){
        if(n%i==0)
        c++;
    }
    if(c==2)
    printf("prime");
    else
    printf("not prime");

        return 0;
}
