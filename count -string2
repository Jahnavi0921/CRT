// count charaters in string without using functions
#include <stdio.h>
#include<string.h>

int main() {
    char str[20];
    int i,count=0;
    scanf("%[^\n]s",&str);
    for(i=0;str[i]!='\0';i++){
        count++;
    }
    printf("%d",count);

    return 0;
}
