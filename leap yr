// read a no and print if it is a leap year or not
#include <stdio.h>
int main() {
    int a;
    scanf("%d",&a);
    if(a%4==0 && (a%100!=0 || a%400==0))//as every 4 years is leap yr and centuary yrs anre not leap years only every 400 years is a leap yr
    {
        printf("leap yr");
    }
    else
    {
        printf("not leap yr");
    }
    
}
