// read 3 digit num & print if it is armstrong or not
#include <stdio.h>

int main() {
    int n,d,t,s=0;
    t=n;
    scanf("%d",&n);
    while(n>0){
        d=n%10;
        n=n/10;
        s=s+d*d*d;
    }
    if(s==t)
    printf("Armstrong no");
    else
    printf("not an armstrong no");

    return 0;
}
