// read a num and print if num is +ve or -ve or 0
#include <stdio.h>
#include<math.h>
int main(){
    int a;
    scanf("%d",&a);
    if(a>0){
        printf("positive");
    }
    else if(a<0){
        printf("negative");
    }
    else{ 
        printf("zero");
    }
}
