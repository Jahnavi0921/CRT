// strcpy without using string functions
#include <stdio.h>
#include<string.h>

int main() {
    char str1[20],str2[20];
    scanf("%[^\n]s",&str1);
    int i;
    for(i=0;str1[i]!='\0';i++){
        str2[i]=str1[i];
    }
    str2[i]='\0';
    printf("%s\n%s",str1,str2);

    return 0;
}
