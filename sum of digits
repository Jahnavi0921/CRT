// read a no & print sum of digits
#include <stdio.h>
#include<math.h>
int main() {
    int n,s=0,d;
    scanf("%d",&n);
    while(n>0){
        d=n%10;
        n=n/10;
        s=s+d;
    }
    printf("%d",s);

    return 0;
}
